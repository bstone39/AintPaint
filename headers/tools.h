#ifndef TOOLS_H
#define TOOLS_H

enum tools{
    BRUSH,
    BUCKET
};

class Brush{
    public:
    
    private:
        //brush type (maybe make it a grid?)
        double size;
        int color;
};

#endif